# День 5 · Урок 1 — теорія

## Бази даних: призначення, види, СКБД, артефакти

> **Вчительський файл.** Учнівський файл дня: `day-05-practice.md`.

---

## Мета дня

Учень розуміє, чим база даних відрізняється від папки з файлами, бачить різницю
між реляційною і нереляційною моделлю, і має на своїй машині працюючий SQL
Server із SSMS.

**Після дня учень має вміти:**

- назвати три речі, які база даних робить, а звичайні файли ні
- сказати, що таке таблиця, рядок, стовпець, первинний і зовнішній ключ
- назвати чотири види NoSQL і сказати, для чого кожен
- підключитись до SQL Server через SSMS

**Потрібно:** SSMS і SQL Server Express на машинах (або встановлюємо разом на
уроці 2 — див. практику), проектор.

> ⚠ **Це найризикованіший день у першому семестрі.** Якщо середовище не
> запрацює, весь блок баз даних (дні 5–18) стоїть. Перевір на одній машині
> **до** уроку: чи встановлюється SQL Server Express, чи підключається SSMS,
> чи є права адміністратора. Якщо прав немає — говори з адміністратором за
> тиждень, не за день.

---

## Хронометраж уроку 1

| Хв | Що |
|---|---|
| 0–8 | Навіщо база, якщо є файли. Шість задач, які вона вирішує |
| 8–20 | **Реляційна модель: таблиця, ключі, зв'язок.** Головна частина, на дошці |
| 20–28 | NoSQL: чотири види, для чого кожен |
| 28–34 | Що таке СКБД і що вона робить, крім зберігання |
| 34–42 | Артефакти: таблиці, ключі, індекси, представлення, обмеження, тригери |
| 42–45 | Анонс практики: ставимо сервер |

---

## Виклад

### 1. Навіщо база, якщо є файли (8 хв)

Почни з провокації: **навіщо база, якщо можна зберігати все у файлі Excel?**
Хай клас поспорить, потім розберіть по пунктах.

| Задача | Чому файл не годиться |
|---|---|
| **Багато користувачів одночасно** | двоє відкрили файл, обидва зберегли — одні зміни зникли |
| **Швидкий пошук** | у файлі на мільйон рядків пошук перебирає все; база йде за індексом |
| **Цілісність** | у файл можна вписати замовлення на клієнта, якого не існує; база не дасть |
| **Права доступу** | файл або видно цілком, або не видно; база дає доступ до окремих таблиць |
| **Транзакції** | переказ грошей — це два дії; якщо між ними вибило світло, у файлі гроші зникнуть |
| **Резервне копіювання** | база вміє відновитись на певний момент у часі |

**Транзакція — найсильніший аргумент**, і його варто розписати: списати з
одного рахунку і додати на інший. Якщо виконалась лише перша половина, гроші
щезли. База гарантує: **або обидві дії, або жодна.**

Де це вже працює: банки (рахунки й транзакції), магазини (товари й замовлення),
лікарні (медичні записи), школа (журнал).

### 2. Реляційна модель (12 хв, головна частина)

Запропонована **Едгаром Коддом у 1970 році** — і досі основа більшості систем
світу.

Уся ідея: **дані — це таблиці, а зв'язки між ними — через ключі.**

🖐 На дошці. Малюємо дві таблиці:

**Учні**

| **id** | прізвище | клас |
|---|---|---|
| 1 | Іваненко | 9-А |
| 2 | Петренко | 9-Б |

**Записи на гуртки**

| id | **учень_id** | гурток |
|---|---|---|
| 1 | 1 | шахи |
| 2 | 1 | робототехніка |

- **Рядок** — один об'єкт (один учень).
- **Стовпець** — одна властивість (прізвище).
- **Первинний ключ** — стовпець, що робить рядок унікальним. Тут `id`.
- **Зовнішній ключ** — стовпець, що вказує на первинний ключ іншої таблиці.
  Тут `учень_id`.

Питання класу, по черзі:

1. **Чому прізвище не годиться як первинний ключ?** Бо однофамільці.
2. **Що станеться, якщо в `учень_id` записати 99, а учня 99 немає?** База
   відмовиться — це й є цілісність. Такий запис називають «сиротою».
3. **Навіщо дві таблиці, чому не одна?** Бо в одного учня кілька гуртків, і в
   одній таблиці прізвище дублювалося б. А дубль — це майбутня розбіжність:
   виправили в одному місці й забули в іншому.

Пункт 3 — головна думка всього блоку. Не поспішай із неї.

### 3. Нереляційні бази (8 хв)

NoSQL не має жорсткої схеми: у документ можна додати нове поле, не переробляючи
таблицю. Легко масштабується на багато серверів.

| Вид | Як зберігає | Приклад | Для чого |
|---|---|---|---|
| Документо-орієнтована | документи JSON | **MongoDB** | каталоги товарів, вміст сайтів |
| Колонкова | по стовпцях, не по рядках | **Cassandra** | аналітика, IoT |
| Графова | вузли й зв'язки | **Neo4j** | соцмережі, рекомендації, пошук шахрайства |
| Ключ-значення | пара «ключ → значення» | **Redis** | кеш, сесії, черги |

Приклад, який добре пояснює графову базу: «друзі друзів мого друга» в
реляційній базі — це JOIN на JOIN на JOIN, а в графовій — природний запит.

⚠ Не давай класу засвоїти «NoSQL новіший, отже кращий». Формула для запису:
**реляційна там, де дані структуровані й помилка дорога** (гроші, медицина);
**NoSQL там, де дані різнорідні, а швидкість і масштаб важливіші** (лента
новин, кеш).

### 4. СКБД (6 хв)

СКБД — програма, яка керує базою. Крім зберігання вона робить:

- створює і змінює структуру
- перевіряє, хто ти, і що тобі дозволено
- тримає цілісність: ключі, обмеження, транзакції
- керує одночасним доступом — блокування, черги
- робить резервні копії й відновлює

Найпоширеніші: **MySQL** (веб, WordPress), **PostgreSQL** (складні системи),
**Microsoft SQL Server** (корпоративні, з ним і працюватимемо), **Oracle**
(банки, держава), **SQLite** (усередині телефона й браузера — вона в тебе вже є,
просто ти цього не знаєш).

⚠ **SQLite варто підкреслити.** Chrome, Firefox, Android, iOS — усі носять
SQLite усередині. Це знімає уявлення, що база даних живе лише на великому
сервері.

Інструменти з графічним інтерфейсом: **SSMS** і Azure Data Studio для SQL
Server, pgAdmin для PostgreSQL, MySQL Workbench, DB Browser для SQLite,
DataGrip і DBeaver універсальні.

### 5. Артефакти реляційної бази (8 хв)

Перелічити з одним прикладом кожен — детально розбиратимемо в наступні дні:

| Артефакт | Що це | Коли розберемо |
|---|---|---|
| **Таблиця** | де лежать дані | день 8 |
| **Первинний ключ** | робить рядок унікальним | день 9 |
| **Зовнішній ключ** | зв'язує таблиці | день 9 |
| **Обмеження** | `NOT NULL`, `UNIQUE`, `CHECK` — правила на дані | день 9 |
| **Індекс** | прискорює пошук | день 10 |
| **Представлення (VIEW)** | збережений запит, схожий на таблицю | день 16 |
| **Тригер** | код, що виконується сам при зміні даних | день 16 |

---

## ⚠ Що піде не так

| Симптом | Що робити |
|---|---|
| «База даних — це просто велика таблиця» | Питання 3 з розділу 2: чому дві таблиці, а не одна |
| Клас плутає рядок і стовпець | Формула: **рядок — один об'єкт, стовпець — одна властивість** |
| «NoSQL кращий, бо новіший» | Формула з кінця розділу 3 |
| Не встановлюється сервер на уроці 2 | Резервний план у практиці: паперове проектування + DB Browser for SQLite |

---

## Урок 2 — практика: відповіді й критерії

Учнівський файл: `day-05-practice.md`.

⚠ **Урок 2 — технічний.** Ставимо середовище на весь блок баз даних. Якщо
машини вже налаштовані, кроки 1–3 займуть 10 хвилин, і решта уроку йде на
кроки 4–5. Якщо ні — установка може з'їсти весь урок, і це нормально.

### Кроки 1–3. Установка й підключення

Порядок: **SQL Server Express → потім SSMS**, не навпаки.

Що чекати:

| Проблема | Причина | Рішення |
|---|---|---|
| Немає прав на установку | шкільна машина під адміністратором | вирішувати до уроку, не на уроці |
| SSMS не бачить сервер | ім'я екземпляра | `localhost\SQLEXPRESS` або просто `.\SQLEXPRESS` |
| `Cannot connect… error 26` | сервер не запущений | `services.msc` → `SQL Server (SQLEXPRESS)` → Start |
| Просить логін і пароль | вибрано SQL Authentication | має бути **Windows Authentication** |

Ознака успіху: у `Object Explorer` розкривається дерево `Databases → System
Databases`. Хай кожен покаже цей екран — так ти дізнаєшся про проблеми зараз, а
не на дні 6.

### Крок 4. Файли проти бази

Очікувані відповіді:

1. **Двоє одночасно зберегли файл** → зміни одного зникли. База: блокування,
   обидві зміни збережуться або друга дочекається.
2. **Замовлення на неіснуючого клієнта** → у файлі запишеться, у базі зовнішній
   ключ не дасть.
3. **Світло вибило посередині переказу** → у файлі гроші зникли, у базі
   транзакція відкотиться.
4. **Мільйон рядків** → файл перебирає все, база йде за індексом.

Найкраща відповідь на 3 містить слово «або все, або нічого».

### Крок 5. Своя схема на папері

Учень бере ідею з дня 1 і проектує 3 таблиці. Що перевіряти:

- ✅ у кожній таблиці **є первинний ключ**
- ✅ є **щонайменше один зовнішній ключ** між таблицями
- ✅ типи хоча б названі словами: число / текст / дата / так-ні
- ⚠ **Головна помилка:** усе в одну таблицю. Ознака — стовпці на кшталт
  `гурток1, гурток2, гурток3`. Питай: «а якщо четвертий?» Це найкорисніша
  розмова уроку.
- ⚠ Друга помилка: замість зовнішнього ключа дублюють прізвище. Питай: «учениця
  вийшла заміж і змінила прізвище. Скільки місць треба виправити?»

Схеми збери — вони будуть основою практики дня 11 (створення власної бази) і
тематичної роботи.

### Оцінювання

| Бали | Ознаки |
|---|---|
| 1–3 | Середовище встановлено з допомогою, крок 4 частково |
| 4–6 | Підключення працює, крок 4 виконано, схема з однієї-двох таблиць без ключів |
| 7–9 | Усе вище + три таблиці, у кожній первинний ключ, є зовнішній ключ |
| 10–12 | Усе + правильно вирішено проблему «багато гуртків в одного учня» окремою таблицею; названо типи даних |

### Що зібрати

Скріншот або показ підключеного SSMS, відповіді кроку 4, схема з кроку 5.
