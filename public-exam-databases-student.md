# Тематична робота № 1 — Бази даних

## Учнівський файл

Два уроки. Комп'ютер потрібен.

| Урок | Що робиш |
|---|---|
| **Урок 1** | проєктуєш базу на папері й створюєш її в SQL Server |
| **Урок 2** | наповнюєш даними й пишеш запити |

Файл здачі — `exam-db.sql`. Один скрипт, із коментарями-номерами частин.

Схему на папері здаєш окремо.

---

## Обери варіант

Твій варіант скаже вчитель. Якщо ні — обери сам.

---

### Варіант А. Прокат велосипедів

Фірма дає велосипеди в прокат погодинно.

Про **клієнтів** треба знати: прізвище, ім'я, телефон, коли зареєструвався.
Телефон у двох клієнтів однаковий бути не може.

Про **велосипеди**: модель, тип (гірський, міський, дитячий), ціна за годину,
чи справний зараз.

Про **прокати**: хто взяв, який велосипед, коли взяв, коли повернув, скільки
заплатив.

⚠ Один клієнт може брати велосипеди багато разів. Один велосипед за сезон
беруть десятки різних людей. **Велосипед може бути ще не повернутий.**

---

### Варіант Б. Шкільна бібліотека

Про **читачів**: прізвище, ім'я, клас, номер читацького квитка (унікальний),
коли записався.

Про **книги**: назва, автор, рік видання, інвентарний номер, кількість
примірників.

Про **видачі**: хто взяв, яку книгу, коли взяв, коли повернув, чи є
заборгованість.

⚠ Той самий читач може брати ту саму книгу кілька разів у різні дати. **Книга
може бути ще на руках.**

---

### Варіант В. Служба доставки їжі *(складніший)*

Про **клієнтів**: прізвище, телефон, адреса доставки.

Про **страви**: назва, категорія, ціна, час приготування, чи є в наявності.

Про **замовлення**: хто замовив, коли, статус, сума, коли доставлено.

⚠ **Одне замовлення містить кілька різних страв, і кожної страви може бути по
кілька порцій.** Подумай уважно: чи достатньо трьох таблиць? Якщо ні — зроби
стільки, скільки потрібно, і поясни в коментарі, чому.

---

# Урок 1

## Частина 1. Схема на папері (15 хв)

Намалюй схему свого варіанта. Обов'язково:

- назви всіх таблиць
- усі стовпці з **типами** (`INT`, `NVARCHAR(50)`, `DECIMAL(10,2)`, `DATE`,
  `DATETIME2`, `BIT`)
- **первинний ключ підкресли** в кожній таблиці
- **зовнішні ключі покажи стрілками** до таблиць, на які вони вказують
- позначь, які поля **обов'язкові**, а які можуть бути порожні

Зразок оформлення:

```
КЛІЄНТИ
  ID          INT           ← ПЕРВИННИЙ КЛЮЧ
  Прізвище    NVARCHAR(50)  обов'язкове
  Телефон     NVARCHAR(20)  унікальний

ПРОКАТИ
  ID          INT           ← ПЕРВИННИЙ КЛЮЧ
  КлієнтID    INT           ← ЗОВНІШНІЙ КЛЮЧ → КЛІЄНТИ.ID
  ...
```

⚠ Перевір себе **трьома питаннями** перед тим, як писати SQL:

1. Чи є в мене стовпці на кшталт `велосипед1`, `велосипед2`, `велосипед3`?
   Якщо є — переробляй, це мусить бути окрема таблиця.
2. Чи дублюю я текст замість зв'язку? Якщо в таблиці прокатів лежить прізвище
   клієнта, а не його `ID` — уяви, що клієнтка змінила прізвище. Скільки рядків
   правити?
3. Чи є поле, яке **осмислено** може бути порожнім? Подумай, чим відрізняється
   «ще не повернув» від «забули записати».

---

## Частина 2. Створення в SQL Server (30 хв)

**2.1.** Створи базу даних і схему в ній.

**2.2.** Створи всі таблиці. Обов'язково мусять бути:

| Що | Скільки |
|---|---|
| `PRIMARY KEY` | у кожній таблиці |
| `FOREIGN KEY` | усі зв'язки з твоєї схеми |
| `NOT NULL` | на всіх обов'язкових полях |
| `UNIQUE` | щонайменше один (телефон, читацький квиток…) |
| `CHECK` | щонайменше один (ціна > 0, рік > 1900…) |
| `DEFAULT` | щонайменше один |

⚠ Порядок створення: батьківські таблиці **раніше** за дочірні.

**2.3. Покажи, що обмеження працюють.** Виконай **три** запити, які мусять
зламатись, і скопіюй текст кожної помилки коментарем:

- порушення `PRIMARY KEY` або `UNIQUE`
- порушення `CHECK`
- порушення `FOREIGN KEY`

---

# Урок 2

## Частина 3. Наповни даними (10 хв)

**3.1.** Додай **щонайменше 5 рядків у кожну таблицю**, `INSERT` пачками.

**3.2.** Один рядок додай так, щоб `DEFAULT` заповнився сам.

⚠ **Дані мусять бути різними.** Якщо всі суми однакові, а всі клієнти брали
рівно по одному велосипеду — запити частини 4 нічого не покажуть, і бали
втратяться там, а не тут. Зроби так, щоб:

- один клієнт мав **два або більше** прокатів
- **один клієнт не мав жодного**
- щось було **ще не повернуто** (поле дати повернення — `NULL`)
- суми були різні

---

## Частина 4. Запити (35 хв)

Після кожного запиту — коментар: **скільки рядків повернуло**.

**4.1.** Простий фільтр за числом (`WHERE` з `>` або `<`).

**4.2.** Сортування за двома стовпцями, один із них — за спаданням.

**4.3.** Пошук за шаблоном через `LIKE`.

**4.4.** Агрегація: одним запитом кількість записів, сума й середнє.

**4.5.** Групування (`GROUP BY`) за якимось стовпцем із підрахунком.

**4.6.** Групування з `HAVING` — залишити лише групи, де більше одного запису.

**4.7.** `JOIN` — виведи дані **з усіх** твоїх таблиць в одному запиті так, щоб
у результаті були зрозумілі людині назви, а не номери. Використай псевдоніми.

**4.8.** `LEFT JOIN` — знайди тих, у кого **немає** жодного пов'язаного запису
(клієнт без прокатів, читач без видач, клієнт без замовлень).

⚠ Тут `WHERE ... = NULL` не працює. Згадай, як правильно.

**4.9.** Підзапит — знайди записи, у яких значення **більше за середнє**.

**4.10.** Робота з `NULL` — виведи те, що **ще не завершено**: велосипеди в
прокаті, книги на руках, замовлення в дорозі.

**4.11.** `UPDATE` **за правилом**:

- спершу `SELECT` із тим самим `WHERE` — подивись, кого зачепить, порахуй рядки
- потім `UPDATE`
- потім `SELECT` для перевірки

⚠ Усі три запити мусять бути у файлі. `UPDATE` без попереднього `SELECT`
коштує балів навіть якщо він правильний.

**4.12.** Транзакція: почни транзакцію, видали кілька рядків, перевір, що їх
немає, потім **скасуй** і перевір, що вони повернулись.

---

## ⭐ Додатково (якщо встиг усе)

**⭐1.** Створи представлення (`VIEW`), яке показує готовий звіт із твого
запиту 4.7, і звернись до нього простим `SELECT`.

**⭐2.** Створи процедуру з параметром, яка повертає дані за заданим значенням
(наприклад усі прокати конкретного клієнта за його `ID`).

**⭐3.** Напиши коментарем, які **два** рішення у своїй схемі ти вважаєш
найважливішими, і що б зламалось, якби ти зробив інакше.

---

## Шпаргалка синтаксису

```sql
CREATE TABLE Схема.Таблиця (
    ID INT PRIMARY KEY,
    Назва NVARCHAR(50) NOT NULL,
    Ціна DECIMAL(10,2) NOT NULL CHECK (Ціна > 0),
    Активний BIT NOT NULL DEFAULT 1,
    ЧужийID INT NOT NULL,
    FOREIGN KEY (ЧужийID) REFERENCES Схема.ІншаТаблиця(ID)
);

INSERT INTO Схема.Таблиця (ID, Назва) VALUES (1, N'Текст'), (2, N'Ще текст');

SELECT a.Назва, b.Інше
FROM Схема.Таблиця AS a
LEFT JOIN Схема.ІншаТаблиця AS b ON a.ЧужийID = b.ID
WHERE b.ID IS NULL;

SELECT Стовпець, COUNT(*) AS К
FROM Схема.Таблиця
WHERE Ціна > 100
GROUP BY Стовпець
HAVING COUNT(*) > 1
ORDER BY К DESC;

BEGIN TRANSACTION;
    DELETE FROM Схема.Таблиця WHERE ID = 1;
ROLLBACK TRANSACTION;
```

Український текст — тип `NVARCHAR` і літера `N` перед лапками: `N'Іваненко'`.

---

## Здати

1. **Схему на папері** — з ключами, стрілками й типами.
2. **Файл `exam-db.sql`** — один скрипт: частини 2, 3, 4 із
   коментарями-номерами, кількостями рядків і текстами помилок із частини 2.3.

⚠ Базу після роботи **не видаляй** — вона знадобиться в кінці курсу, коли ми
підключимось до неї з Python.
