# День 27 · Урок 1 — теорія

## Складність алгоритмів. Асимптотична складність. Big O

> **Вчительський файл.** Учнівський файл дня: `day-27-practice.md`.
> *(У початковій нумерації лекцій це був урок 30.)*

---

## Мета дня

Учень уміє оцінити, як програма поводитиметься на великих даних, **не
запускаючи її** — просто подивившись на цикли.

**Після дня учень має вміти:**

- пояснити, що описує Big O і чому константи в ній відкидають
- визначити складність простого коду за кількістю вкладених циклів
- назвати O(1), O(log n), O(n), O(n log n), O(n²), O(2ⁿ) з прикладом кожного
- сказати, чому бінарний пошук швидший за лінійний і за якої умови він працює
- виміряти час виконання й показати, що теорія збігається з практикою

**Потрібно:** Python і VS Code, проектор.

> **Головна перевага цього дня:** учні вже міряли O(n²) руками на дні 21, коли
> рахували порівняння в бульбашці. Почни саме з тієї таблиці — тоді Big O буде
> назвою для вже пережитого, а не новим абстрактним поняттям.

---

## Хронометраж уроку 1

| Хв | Що |
|---|---|
| 0–7 | Повернення до таблиці з дня 21. Що ми тоді виміряли |
| 7–16 | Що таке Big O і чому відкидають константи |
| 16–32 | **Шість класів складності** з прикладами коду |
| 32–40 | **Бінарний пошук** — головний приклад O(log n) |
| 40–43 | Просторова складність |
| 43–45 | Анонс практики |

---

## Виклад

### 1. Що ми вже виміряли (7 хв)

Виклич на дошку таблицю з дня 21 — кількість порівнянь у бульбашці:

| Довжина списку | Порівнянь |
|---|---|
| 10 | 45 |
| 20 | 190 |
| 50 | 1 225 |
| 100 | 4 950 |

Питання класу: **список побільшав удесятеро — робота побільшала в скільки?**
Приблизно в сто. Тобто **у квадрат**.

Ось цій закономірності є назва: **O(n²)**. Сьогодні ми навчимось бачити її в
коді, не роблячи вимірів.

Навіщо це знати:

- **вибір алгоритму.** Той самий результат можна отримати за секунду або за
  годину.
- **планування.** Програма працює на 100 записах — а на мільйоні?
- **на співбесідах питають саме це**, і не випадково.

### 2. Big O і чому відкидають константи (9 хв)

**Big O описує, як зростає час роботи при зростанні обсягу даних.** Не скільки
секунд, а **як швидко росте**.

Три речі, які Big O свідомо ігнорує:

1. **Константи.** `3n` і `n` — обидва O(n).
2. **Менш значущі доданки.** `n² + 5n + 100` → O(n²).
3. **Швидкість комп'ютера.**

Питання класу: **чому це правильно — відкидати?**

Розбери на числах. Алгоритм A робить `100n` операцій, алгоритм B робить `n²`.

| n | A: 100n | B: n² | Хто швидший |
|---|---|---|---|
| 10 | 1 000 | 100 | **B** |
| 100 | 10 000 | 10 000 | однаково |
| 1 000 | 100 000 | 1 000 000 | **A** |
| 1 000 000 | 100 000 000 | 1 000 000 000 000 | **A**, у 10 000 разів |

На малих даних константа вирішує. **На великих — перемагає клас складності, і
перемагає з розгромом.** Тому Big O дивиться саме на нього.

⚠ Практичний висновок, який варто сказати: для 20 елементів бульбашка нормальна.
Питання завжди в тому, **скільки даних буде через рік**.

### 3. Шість класів складності (16 хв — головна частина)

Розбирай кожен із кодом і питай клас, скільки разів виконається тіло.

**O(1) — константна.** Не залежить від обсягу даних.

```python
def get_first(numbers):
    return numbers[0]
```

Список на 10 елементів чи на 10 мільйонів — одна дія. Сюди ж: `len(list)`,
доступ до значення словника за ключем, `append`.

⚠ Зв'яжи з днем 22: **словник шукає за ключем за O(1)**, а список — перебором
за O(n). Ось справжня причина, чому словники такі корисні.

**O(log n) — логарифмічна.** З кожним кроком задача зменшується **вдвічі**.

Приклад — бінарний пошук (розділ 4). Число кроків: для мільйона елементів —
близько **двадцяти**.

**O(n) — лінійна.** Один прохід по даних.

```python
def find_min(numbers):
    min_num = numbers[0]        # O(1)
    for num in numbers:         # n разів
        if num < min_num:       # O(1)
            min_num = num
    return min_num
```

Разом O(n). Сюди ж: `sum()`, `max()`, `in` для списку, читання файлу построчно.

**O(n log n) — лінійно-логарифмічна.** Найкраще, що можливо для сортування
порівняннями. Швидке сортування, сортування злиттям, вбудований `sort()`.

**O(n²) — квадратична.** Два вкладені цикли по тих самих даних.

```python
def bubble_sort(numbers):
    n = len(numbers)
    for i in range(n):              # n
        for j in range(n - i - 1):  # ≈ n
            if numbers[j] > numbers[j + 1]:
                numbers[j], numbers[j + 1] = numbers[j + 1], numbers[j]
```

**O(2ⁿ) — експоненційна.** Катастрофа. Рекурсивні числа Фібоначчі без
оптимізації:

```python
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)
```

⚠ Покажи це на проекторі: `fib(30)` рахується помітно, `fib(40)` — секунди
десятки, `fib(50)` не дочекаєшся. Це найсильніша демонстрація уроку.

**Правило, яке дозволяє оцінювати код на око** — виписати на дошку:

| Що бачу в коді | Складність |
|---|---|
| немає циклів | O(1) |
| один цикл по даних | O(n) |
| цикл у циклі по тих самих даних | O(n²) |
| три вкладені цикли | O(n³) |
| дані діляться навпіл щокроку | O(log n) |
| сортування всередині | не менше O(n log n) |

⚠ Обережно з пасткою: цикл усередині циклу **не завжди** O(n²). Якщо
внутрішній робить фіксовану кількість кроків (`range(10)`), це все ще O(n).
Питання не «скільки циклів», а «скільки разів виконається тіло **залежно від
n**».

### 4. Бінарний пошук (8 хв)

Головний приклад O(log n), і його варто розібрати на дошці.

🖐 **Гра «вгадай число» — тільки навпаки.** Загадай число від 1 до 100 і хай
клас вгадує, а ти кажеш «більше» або «менше».

Учні майже завжди вгадують за 7 спроб, називаючи середину. Це і є бінарний
пошук, і 7 ≈ log₂(100).

```python
def binary_search(numbers, target):
    low = 0
    high = len(numbers) - 1
    while low <= high:
        mid = (low + high) // 2
        if numbers[mid] == target:
            return mid
        elif numbers[mid] < target:
            low = mid + 1
        else:
            high = mid - 1
    return -1
```

Порівняння на числах — це і є суть уроку:

| Кількість елементів | Лінійний пошук O(n) | Бінарний O(log n) |
|---|---|---|
| 100 | до 100 кроків | 7 |
| 1 000 | до 1 000 | 10 |
| 1 000 000 | до 1 000 000 | **20** |
| 1 000 000 000 | до мільярда | **30** |

⚠⚠ **Головна умова, без якої нічого не працює: список мусить бути
відсортований.** Питання класу: **якщо список не відсортований, а шукати треба
один раз — що вигідніше?** Лінійний пошук: відсортувати коштує O(n log n), а це
дорожче за один прохід O(n). Якщо ж шукати доведеться тисячі разів —
сортування окупиться.

Це найважливіша думка дня: **вибір алгоритму залежить не лише від даних, а й
від того, що ти з ними робитимеш.**

Зв'яжи з блоком баз даних: **індекс на дні 10 — це рівно те саме.** Він коштує
місця й уповільнює запис, зате перетворює перебір усієї таблиці на пошук за
кілька кроків. `Index Seek` проти `Table Scan` — це O(log n) проти O(n).

### 5. Просторова складність (3 хв)

Big O описує не лише час, а й **пам'ять**.

- Бульбашка: сортує на місці → **O(1) додаткової пам'яті**
- Сортування злиттям: створює копії половин → **O(n) пам'яті**
- Швидке сортування у варіанті з дня 21 (зі списковими виразами) теж O(n)

⚠ Класичний компроміс: **час можна купити пам'яттю й навпаки.** Приклад, який
учні вже бачили — тимчасова таблиця в SQL на дні 14: витратили пам'ять, зате
не рахували групування двічі.

---

## ⚠ Що піде не так

| Симптом | Причина | Що робити |
|---|---|---|
| «O(n²) означає, що це погано» | плутають клас із придатністю | на 20 елементах бульбашка нормальна; питання в обсязі |
| Рахують кількість циклів замість залежності від n | `for j in range(10)` вважають за O(n) | питання: «залежить від n чи ні?» |
| Бінарний пошук не працює | список не відсортований | обов'язкова умова |
| `fib(50)` завісив машину | O(2ⁿ) | `Ctrl+C`; не пробувати понад 35 |
| Не бачать різниці на малих даних | вона й не мусить бути видна | міряти на 10 000+ |

---

## Урок 2 — практика: відповіді й критерії

Учнівський файл: `day-27-practice.md`. Файли `day27_1.py` … `day27_5.py`.

### Відповіді

**Крок 1. Визнач складність на око**

| Код | Складність |
|---|---|
| `return numbers[0]` | O(1) |
| один `for` по списку | O(n) |
| два вкладені `for` по списку | O(n²) |
| `for` по списку + `for i in range(10)` усередині | **O(n)** — пастка |
| `numbers.sort()` потім один `for` | O(n log n) |
| три вкладені `for` | O(n³) |
| `if x in my_dict` | O(1) |
| `if x in my_list` | O(n) |

⚠ Останні два — найцінніші. Учень, який назвав різницю правильно, розуміє, чому
на дні 22 словник називався найкориснішою структурою.

**Крок 2. Вимірювання часу**

```python
import time

start = time.time()
# код
print(f"Час: {time.time() - start:.4f} с")
```

Порівняння лінійного й бінарного пошуку на списку в мільйон елементів. Різниця
буде в тисячі разів — саме те, що потрібно побачити.

⚠ Лінійний пошук найгіршого випадку — шукати елемент, якого **немає** в списку,
або останній. Якщо учень шукає перший елемент, різниці не побачить, і це
привід поговорити про «найгірший випадок».

**Крок 3. Бінарний пошук**

Реалізація з виклада плюс лічильник кроків. Очікуване:

| Довжина | Кроків бінарного | Кроків лінійного (найгірше) |
|---|---|---|
| 100 | ≤ 7 | 100 |
| 1 000 | ≤ 10 | 1 000 |
| 100 000 | ≤ 17 | 100 000 |
| 1 000 000 | ≤ 20 | 1 000 000 |

**Крок 4. Фібоначчі**

```python
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)
```

Заміри: `fib(25)` — частка секунди, `fib(30)` — помітно, `fib(35)` — секунди.

⚠ Не давай запускати понад 35 — деякі машини стануть надовго.

Потім — варіант із запам'ятовуванням:

```python
def fib_fast(n, memo={}):
    if n <= 1:
        return n
    if n in memo:
        return memo[n]
    memo[n] = fib_fast(n - 1) + fib_fast(n - 2)
    return memo[n]
```

`fib_fast(100)` рахується миттєво. **Це найсильніше враження дня.** Обов'язковий
висновок: витратили пам'ять (словник), купили час — той самий компроміс.

**Крок 5. Свій алгоритм і його оцінка**

Учень пише алгоритм обробки даних і **сам оцінює складність із поясненням по
рядках**. Це те, що вимагає лекція («складення алгоритму та оцінка складності»).

Приймається будь-яка задача, у якій є цикл. Оцінюється **обґрунтування**, а не
буква: «один прохід по списку → O(n)», «для кожного елемента шукаю в іншому
списку → O(n·m)».

### Оцінювання

| Бали | Ознаки |
|---|---|
| 1–3 | Класи складності перелічені, приклади впізнаються |
| 4–6 | Крок 1 переважно правильно; час виміряно |
| 7–9 | Кроки 1–3; **бінарний пошук працює**, лічильник кроків показує log-залежність; названо умову відсортованості |
| 10–12 | Усе + Фібоначчі з мемоізацією й висновком про обмін пам'яті на час, або власний алгоритм із поясненою оцінкою |

### Що зібрати

`day27_1.py` … `day27_5.py` із таблицями замірів у коментарях.
